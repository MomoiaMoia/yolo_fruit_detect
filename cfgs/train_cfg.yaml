trainer:
  device: 'cuda' # 'cpu' or 'cuda'
  lr: 0.0001
  epochs: 300
  log_dir: 'logs'
  ckpt_dir: 'ckpts'
  dataset_dir: './strawberry_cls'

  metric_every: 1
  save_every: 1

dataset:
  batch_size: 32
  num_workers: 4