trainer:
  device: 'cuda' # 'cpu' or 'cuda'
  lr: 0.01
  epochs: 1000
  log_dir: 'logs'
  ckpt_dir: 'ckpts'
  dataset_dir: './strawberry_cls'

  metric_every: 20
  save_every: 50

dataset:
  batch_size: 64
  num_workers: 4